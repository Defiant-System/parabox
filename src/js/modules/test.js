
let Test = {
	init(APP) {
		// setTimeout(() => window.find(`.toolbar-tool_[data-arg="2"]`).trigger("click"), 100);
		// setTimeout(() => Game.checkWin(), 1000);
		// setTimeout(() => window.find(`.exit`).addClass("ready"), 1000);
		// setTimeout(() => APP.content.addClass("game-won"), 1500);
	}
};
